## LLM OCR with MinerU

# Instal MinerU
```
pip install --upgrade pip
pip install uv
uv pip install -U "mineru[all]"
```

# Run MinerU as API:
```
mineru-api --host 127.0.0.1 --port 8000 --enable-vlm-preload true
```

# Run conversion script on the MSR document database



